# Gates — 2026-09-21

오늘의 개선: **arabold/docs-mcp-server 도구 설치(b)** — 2026-09-20 이월 1순위 집행.
조건(어제 원장 명기): PostHog opt-out 강제 · 키워드 모드(임베딩 키 0) 또는 Ollama 로컬 한정 · MCP 미등록(mcp-scanner 게이트 전) · Windows npx 스모크.

## G1 — 설치 전 재실측 (추정 금지)

- [x] gh api 읽기 전용으로 어제 수치 재확인: LICENSE 본문 MIT·최신 릴리스·pushed·이상 신호 없음
- [x] 텔레메트리 opt-out 방법을 repo 문서/코드에서 실측 확인 (env 이름·CLI 플래그 — 추정 금지)
- 증거 (2026-09-21 background agent, gh api GET+raw fetch만, clone/실행 0):
  - ★1,751(+2)·pushed 2026-09-20T22:25:44Z 활발·open_issues 68·릴리스 v3.1.0(08-29) 최신 유지·LICENSE 본문 표준 MIT "(c) 2025 Andre Rabold" NC 0·spdx 일치 — 어제 실측과 정합, 이상 신호 신규 0
  - 텔레메트리 opt-out 원문(docs/infrastructure/telemetry.md HTTP 200): CLI `--no-telemetry` = "Disable all telemetry for current session" / env `DOCS_MCP_TELEMETRY=false` = "Global disable telemetry collection" / YAML `app.telemetryEnabled: false`. 수집 범위 원문 "Never collects URLs, document content, search queries, or authentication tokens" 재확인. Installation ID는 `~/.local/share/docs-mcp-server/installation.id`
  - CLI 형식 원문: `npx @arabold/docs-mcp-server@latest scrape <lib> <url>` / `search <lib> "<query>" --output yaml` / MCP 서버 모드는 :6280 SSE(오늘 미사용). Node.js 22+ 요구. README 브랜딩 "Grounded Docs"로 리네이밍(패키지명 동일)

## G2 — 비침습 설치 경계

- [x] MCP 미등록: `~/.claude.json`에 docs-mcp-server 0건 (설치 전후 동일)
- [x] `~/.claude/settings.json` SHA256 설치 전후 불변
- [x] 임베딩 API 키 미설정 상태(키워드 모드)로만 실행 — 외부 LLM/임베딩 호출 표면 0
- 증거 (2026-09-21 background agent 스모크, Windows 네이티브):
  - `~/.claude.json` 내 `docs-mcp-server` 문자열 사전·사후 모두 0건 — `claude mcp add` 미실행 (mcp-scanner 게이트 전 MCP 미등록 조건 준수)
  - settings.json SHA256 사전=사후 `633CE7CC...B036` 불변
  - 부모 셸에 OPENAI_API_KEY·GOOGLE_API_KEY 실존 발견 → 매 실행 세션에서 `Remove-Item Env:` 후 `KEYS_REMAINING: 0` 확인하고 실행 (FTS 키워드 모드 보장)
  - 주의 채록: 자동 생성 config 기본값에 `embeddingModel: text-embedding-3-small` — 셸에 키가 있으면 임베딩 경로 자동 활성화 가능. 키워드 전용 운용 조건을 entity 사용 지침에 명기

## G3 — Windows npx 스모크 (키워드 모드·텔레메트리 off)

- [x] 텔레메트리 opt-out 적용 상태로 scrape(소형 로컬/공개 문서) → search 왕복 실측: 결과가 인덱싱 원문과 일치
- [x] 데이터가 로컬 저장소 경로에만 기록됨 확인 (경로 실측)
- [x] 프로세스 종료 후 잔존 프로세스 0
- 증거 (2026-09-21 background agent 스모크):
  - 버전 실측: `npx -y @arabold/docs-mcp-server@latest --version` → `3.1.0`
  - 텔레메트리 이중 opt-out: env `DOCS_MCP_TELEMETRY=false` + CLI `--no-telemetry`(우선순위 최상위) 전 실행 적용. 추가 발견: 자동 생성 `%APPDATA%\docs-mcp-server\Config\config.yaml`이 `telemetryEnabled: true`로 영속화됨 → 이월 조건 "opt-out 강제"에 따라 `false`로 수정해 영속 opt-out 완료
  - scrape 왕복: 임시 md(고유 토큰 CCEVOLVE0921SMOKE) → 1차 `AccessPolicyError: outside the configured allowed roots`(기본 allowed root=$DOCUMENTS만 — 로컬 파일 접근이 기본 차단되는 보안 게이트 실측, 긍정 신호) → `DOCS_MCP_SCRAPER_SECURITY_FILE_ACCESS_ALLOWED_ROOTS` 임시 폴더 한정 허용 후 `Successfully scraped 1 pages` → `search smoketest "CCEVOLVE0921SMOKE" --output json`으로 원문 전문 회수(FTS 키워드 모드) → list: 1 library·documentCount 1·completed
  - 데이터 로컬 저장 실측: `%LOCALAPPDATA%\docs-mcp-server\Data\documents.db`(SQLite 156KB) + `installation.id`(36B), config는 `%APPDATA%\docs-mcp-server\Config\config.yaml` — 전부 로컬 경로
  - 사후: :6280 리스닝 0·잔존 프로세스 0·임시 md 폴더 삭제 확인(Test-Path False)

## G4 — 검증·기록

- [x] 오늘 트렌드 후보(상위 5~10건) 검토·판정 후 seenRepos append (채택·기각 불문)
- [x] `node evolve/health.mjs` 9/9 통과 + state.json 갱신(lastRun·runs append·appliedImprovements `tool:arabold/docs-mcp-server-install`·consecutiveFailures 0) + wiki entity 설치 완료 절 반영 + 커밋·푸시
- 증거:
  - dedup 사전 확인: 후보 9건 전부 state.json(seenRepos·원장)·~/.wiki 0건 (2026-09-21 세션 Grep 실측) → 검토 후 기각 9건 seenRepos append 316→325, 사유는 runs/appliedImprovements note 채록
  - wiki 반영 (wiki-librarian 산출 후 디스크 grep 재검증 보고): entities/docs-mcp-server.md 설치(b) 완료 절 + entities/fast-jev-compaction.md 정정 절에 gentle-shell README "stargazers API admin 한정(2026-06-30)" 제3자 교차 증거 append + concepts/self-evolving-daily-loop.md evolve 39차 절("세션 opt-out ≠ 영속 opt-out" 교훈) + index-thin.md 이력 1줄(신규 페이지 0·카운트 불변 Entities 89/총 ~300) · 부수: _librarian/log.md 파일 부재 drift 해소(신규 생성)
  - `node -e JSON.parse(state.json)` → STATE_JSON_OK · `node evolve/health.mjs` → 9항목 전부 ok, ALL_HEALTHY (커밋 직전 실측)
  - state.json: lastRun 2026-09-20T22:38:47Z · runs 2026-09-21 append(30개) · appliedImprovements `tool:arabold/docs-mcp-server-install` append · consecutiveFailures 0
  - 본 개선 커밋: `0a05e5b` (푸시 완료, 864d5d6..0a05e5b master)
