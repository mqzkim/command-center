# Gates: daily 2026-08-23 — trends 후보 소비 버그 수정 (자기수정)

발견: trends.mjs가 검색 결과를 검토 전에 state.seenRepos에 넣고(82건) 파일엔 25건만 기록 → 57건 영구 유실, 이후 매일 new=0 (실측: 2026-08-23 로그 `new=0 dup_seen=82`). 플레이북 §3이 영원히 빈 후보만 본다.
수정: seenRepos = "플레이북이 검토(채택/기각)한 repo"로 의미 변경. trends.mjs는 seenRepos를 읽기만 하고 쓰지 않으며 후보 전량(≤60)을 기록. 검토 전이던 seenRepos 82건은 비운다.
§6 규칙: 오늘은 자기수정만(기능 개선 없음).

- [x] G1: 수정된 trends.mjs 문법 통과
  CHECK: node --check evolve/trends.mjs
  EXPECT: exit 0
  EVIDENCE: exit 0 (실측 2026-08-23)

- [x] G2: 스캔 재실행 시 후보가 복원되고(new ≥ 20) seenRepos가 변하지 않음
  CHECK: node evolve/trends.mjs && node -e "const s=require('./evolve/state.json');const t=require('./evolve/trends-latest.json');console.log('seen',s.seenRepos.length,'cand',t.candidates.length)"
  EXPECT: /TRENDS_OK .* new=[2-9][0-9]/ 그리고 seen 0
  EVIDENCE: TRENDS_OK scanned=84 new=82 dup_wiki=2 dup_seen=0 · seen 0 cand 60 (파일 캡 60 — 전체 82 중 상위 60 기록, 나머지는 다음 스캔에서 재등장하므로 유실 아님)

- [x] G3: 멱등성 — 연속 2회 실행해도 new 수가 동일 (소비 없음 증명)
  CHECK: node evolve/trends.mjs | grep -o 'new=[0-9]*' (2회)
  EXPECT: 두 값 동일
  EVIDENCE: 1회차 new=82 → 2회차 new=82. seenRepos 0 유지 — 스캔이 후보를 소비하지 않음

- [x] G4: health 전 항목 통과
  CHECK: node evolve/health.mjs
  EXPECT: /ALL_HEALTHY|HEALED/
  EVIDENCE: 9종 ok, ALL_HEALTHY (evolve-scripts-syntax 포함)
