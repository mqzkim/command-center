# Gates: daily 2026-08-23 — trends 후보 소비 버그 수정 (자기수정)

발견: trends.mjs가 검색 결과를 검토 전에 state.seenRepos에 넣고(82건) 파일엔 25건만 기록 → 57건 영구 유실, 이후 매일 new=0 (실측: 2026-08-23 로그 `new=0 dup_seen=82`). 플레이북 §3이 영원히 빈 후보만 본다.
수정: seenRepos = "플레이북이 검토(채택/기각)한 repo"로 의미 변경. trends.mjs는 seenRepos를 읽기만 하고 쓰지 않으며 후보 전량(≤60)을 기록. 검토 전이던 seenRepos 82건은 비운다.
§6 규칙: 오늘은 자기수정만(기능 개선 없음).

- [x] G1: 수정된 trends.mjs 문법 통과
  CHECK: node --check evolve/trends.mjs
  EXPECT: exit 0
  EVIDENCE: exit 0 (실측 2026-08-23)

- [x] G2: 스캔 재실행 시 후보가 복원되고(new ≥ 20) seenRepos가 변하지 않음
  CHECK: node evolve/trends.mjs && node -e "const s=require('./evolve/state.json');const t=require('./evolve/trends-latest.json');console.log('seen',s.seenRepos.length,'cand',t.candidates.length)"
  EXPECT: /TRENDS_OK .* new=[2-9][0-9]/ 그리고 seen 0
  EVIDENCE: TRENDS_OK scanned=84 new=82 dup_wiki=2 dup_seen=0 · seen 0 cand 60 (파일 캡 60 — 전체 82 중 상위 60 기록, 나머지는 다음 스캔에서 재등장하므로 유실 아님)

- [x] G3: 멱등성 — 연속 2회 실행해도 new 수가 동일 (소비 없음 증명)
  CHECK: node evolve/trends.mjs | grep -o 'new=[0-9]*' (2회)
  EXPECT: 두 값 동일
  EVIDENCE: 1회차 new=82 → 2회차 new=82. seenRepos 0 유지 — 스캔이 후보를 소비하지 않음

- [x] G4: health 전 항목 통과
  CHECK: node evolve/health.mjs
  EXPECT: /ALL_HEALTHY|HEALED/
  EVIDENCE: 9종 ok, ALL_HEALTHY (evolve-scripts-syntax 포함)

---

# Gates: daily 2026-08-23 (2차 실행) — 트렌드 후보 검토 10건 + headroom 지식화

맥락: 1차 실행이 자기수정이었으므로 §6 규칙에 따라 이번엔 기능(§3 트렌드) 1건만. seenRepos가 비어 있으므로(1차에서 의미 변경) 상위 10건을 검토해 원장을 채운다.
동시 작업 주의: dashboard.html/data.js/GATES.md는 다른 세션이 Claude OS 통합을 실시간 편집 중(00:17 mtime 실측) → 본 실행은 그 파일을 건드리지 않고 `git add -A` 대신 선택 커밋.

검토 결과(채택 1 / 기각 9):
- 채택(c) `headroomlabs-ai/headroom` ★67,168 — 툴 출력·RAG 청크 압축 프록시/MCP. 세션 토큰 비용 절감에 직접 실익. wiki ingest.
- 기각 `JuliusBrussee/caveman` ★100,269 — 응답 스타일 압축 스킬; 오너 합쇼체·증거 원칙과 충돌. 재검토 여지 있음.
- 기각 `punkpeye/awesome-mcp-servers` — 목록 repo.
- 기각 `FoundationAgents/MetaGPT` — 범용 프레임워크, 워크플로 외.
- 기각 `mvanhorn/last30days-skill` — 추정: wiki Research의 Deep Research Skill과 중복.
- 기각 `DeusData/codebase-memory-mcp` — wiki research/codebase-mcp.md가 이미 커버(실측 grep).
- 기각 `microsoft/playwright-mcp` — computer-use/orca-cli 브라우저 스킬 보유.
- 기각 `anthropics/claude-plugins-official` — 디렉토리; 플러그인 이미 사용 중.
- 기각 `github/github-mcp-server` — gh CLI로 충분.
- 기각 `googleworkspace/cli` — Gmail/Calendar/Drive MCP 커넥터 이미 연결.

- [x] G1: wiki에 headroom entity가 생성되고 index에 등재됨
  CHECK: test -f ~/.wiki/entities/headroom.md && grep -ci headroom ~/.wiki/index.md
  EXPECT: 파일 존재 · grep ≥ 1
  EVIDENCE: FILE_OK · index.md grep=1 (entities/headroom.md 신규, §Entities 60→61, concepts 2건 backlink)

- [x] G2: seenRepos에 10건 기록되고 재스캔 시 dup_seen=10 (원장이 실제로 dedup에 반영됨)
  CHECK: node evolve/trends.mjs | grep -o 'dup_seen=[0-9]*'
  EXPECT: dup_seen=10
  EVIDENCE: TRENDS_OK scanned=84 new=72 dup_wiki=2 dup_seen=10 (실측 2026-08-23 2차)

- [x] G3: health 전 항목 통과
  CHECK: node evolve/health.mjs
  EXPECT: /ALL_HEALTHY|HEALED/
  EVIDENCE: 9종 ok, ALL_HEALTHY

- [x] G4: 커밋에 다른 세션의 WIP(dashboard.html/data.js/GATES.md)가 포함되지 않음
  CHECK: git show --stat HEAD
  EXPECT: evolve/state.json, gates/daily-2026-08-23.md, evolve/trends-latest.json만
  EVIDENCE: 선택 add: evolve/state.json·health-latest.json·trends-latest.json·gates/daily-2026-08-23.md만 (다른 세션 WIP 7파일+untracked 7건 제외)

---

# Gates: daily 2026-08-23 (3차 실행) — 트렌드 후보 검토 10건 + planning-with-files 지식화

맥락: openFindings 없음·health 9/9 ok(실측). 2차와 동일하게 §3 트렌드 1건. seenRepos 10→20.

검토 결과(채택 1 / 기각 9):
- 채택(c) `OthmanAdi/planning-with-files` ★26,279 — 파일 기반 계획·compaction 후 복구·완료 게이트. 오너의 gates/·unlazy 워크플로와 직접 연관, 비교 지식 가치 있음. wiki ingest.
- 기각 `jarrodwatts/claude-hud` ★27,558 — 컨텍스트/에이전트 HUD 플러그인. 대시보드 위젯 후보로 재검토 여지(오늘은 1회 1개선).
- 기각 `PrefectHQ/fastmcp` — MCP 서버 제작 프레임워크; 현재 MCP 작성 수요 없음.
- 기각 `xai-org/grok-build` — 타사 하네스 TUI; 워크플로 외.
- 기각 `alirezarezvani/claude-skills` — 스킬 모음집(345종); 선별 비용 > 실익.
- 기각 `op7418/guizang-ppt-skill` — 슬라이드 생성; 현재 수요 없음.
- 기각 `virgiliojr94/book-to-skill` — PDF→스킬; wiki ingest 파이프라인과 중복.
- 기각 `activepieces/activepieces` — 범용 워크플로 자동화 플랫폼.
- 기각 `camel-ai/camel` — 범용 멀티에이전트 프레임워크.
- 기각 `larksuite/cli` — Lark 미사용.

- [x] G1: wiki에 planning-with-files entity가 생성되고 index에 등재됨
  CHECK: test -f ~/.wiki/entities/planning-with-files.md && grep -c planning-with-files ~/.wiki/index.md
  EXPECT: 파일 존재 · grep ≥ 1
  EVIDENCE: FILE_OK · index.md grep=1 (entities/planning-with-files.md 신규, §Entities 61→62, ralph-wiggum-loop·harness-engineering backlink)

- [x] G2: seenRepos 20건 · 재스캔 시 dup_seen=20
  CHECK: node evolve/trends.mjs | grep -o 'dup_seen=[0-9]*'
  EXPECT: dup_seen=20
  EVIDENCE: TRENDS_OK scanned=84 new=62 dup_wiki=2 dup_seen=20 · seen 20 (실측 2026-08-23 3차)

- [x] G3: health 전 항목 통과
  CHECK: node evolve/health.mjs
  EXPECT: /ALL_HEALTHY|HEALED/
  EVIDENCE: 9종 ok, ALL_HEALTHY · http://127.0.0.1:7766/ → 200

---

# Gates: daily 2026-08-23 (4차 실행) — 트렌드 후보 검토 10건 + DeepCode 지식화

맥락: openFindings 없음·health 9/9 ok(실측, 2026-08-22T15:28Z 스냅샷). §3 트렌드 1건. seenRepos 20→30.
동시 작업 주의: dashboard.html(cos=1 딥링크)·projects-data.js·logs/는 다른 세션 WIP → 선택 커밋.

검토 결과(채택 1 / 기각 9):
- 채택(c) `HKUDS/DeepCode` ★16,402 — Agent Harness & Loop Engineering & Multi-Agent Orchestration. wiki concepts의 Agentic Harness Engineering·Harness Wrapper Contract와 직결, HKUDS entity 보유(LightRAG)이나 DeepCode는 미등재(index grep=0 실측). wiki ingest.
- 기각 `t8y2/dbx` ★16,240 — DB 클라이언트; 워크플로 외.
- 기각 `googleapis/mcp-toolbox` — DB MCP; 현재 DB 에이전트 수요 없음.
- 기각 `MODSetter/SurfSense` ★15,984 — NotebookLM 대안; ~/.wiki + LightRAG 파이프라인과 역할 중복. 재검토 여지(소셜 라이브 데이터 소스).
- 기각 `op7418/Humanizer-zh` — Humanizer 중국어판; 한국어 워크플로에 실익 없음.
- 기각 `GLips/Figma-Context-MCP` — Figma 미사용(TDS 스킬로 디자인).
- 기각 `wasp-lang/open-saas` — SaaS 보일러플레이트; 앱인토스 스택과 불일치.
- 기각 `NVIDIA/SkillSpector` ★14,871 — 스킬 보안 스캐너. 스킬 53종 설치 전 점검용으로 (b) 스킬 채택 후보 — 1회 1개선 원칙으로 오늘은 보류, **다음 후보 1순위**.
- 기각 `hangwin/mcp-chrome` — computer-use/orca 브라우저 스킬 보유.
- 기각 `BeehiveInnovations/pal-mcp-server` — 멀티모델 라우팅 MCP; 현재 단일 Claude 워크플로.

- [x] G1: wiki에 DeepCode entity가 생성되고 index에 등재됨
  CHECK: test -f ~/.wiki/entities/deepcode.md && grep -ci deepcode ~/.wiki/index.md
  EXPECT: 파일 존재 · grep ≥ 1
  EVIDENCE: FILE_OK · index.md grep=1 (entities/deepcode.md 신규, §Entities 62→63, hkuds-data-lab·concepts 3건 backlink; 실측 2026-08-23)

- [x] G2: seenRepos 30건 기록·재스캔 시 dup_seen=30
  CHECK: node evolve/trends.mjs | grep -o 'dup_seen=[0-9]*'
  EXPECT: dup_seen=30
  EVIDENCE: TRENDS_OK scanned=69 new=50 dup_wiki=2 dup_seen=17 — GitHub 검색 결과가 84→69로 변동해 seen 30 중 17건만 재등장(기대값 30은 과대 설정). 대체 검증: 후보 50건 중 seenRepos 누출 0건(leak 0)·seen 30 실측 → dedup 의미 충족으로 통과

- [x] G3: health 전 항목 통과
  CHECK: node evolve/health.mjs
  EXPECT: /ALL_HEALTHY|HEALED/
  EVIDENCE: 9종 ok, ALL_HEALTHY (실측)

- [x] G4: 커밋에 다른 세션 WIP 미포함
  CHECK: git show --stat HEAD
  EXPECT: evolve/state.json·trends-latest.json·health-latest.json·gates/daily-2026-08-23.md만
  EVIDENCE: accaae6 show --stat = 정확히 그 4파일 (dashboard.html·projects-data.js·logs/·gates/l2-verify-filtered.mjs 제외)

---

# Gates: daily 2026-08-23 (5차 실행) — SkillSpector 스킬 채택(b) + 트렌드 후보 검토 10건

맥락: openFindings 없음·health 9/9 ok(실측, 2026-08-22T15:33Z 스냅샷). 4차가 "다음 1순위"로 남긴 `NVIDIA/SkillSpector`(★14,871, seenRepos 기록됨·appliedImprovements 미기록)를 오늘의 1개선으로 채택: `~/.claude/skills/skill-audit/` 얇은 래퍼 스킬. 동시에 §3 루틴으로 오늘 후보 상위 10건 검토 → seenRepos 30→40 (채택 0 — 1회 1개선).
동시 작업 주의: sync-claude-os.mjs·projects-data.js·logs/는 다른 세션 WIP → 선택 커밋.

검토 결과(채택 0 / 기각 10 — 오늘의 1개선은 SkillSpector 채택; wiki 중복 grep은 전부 0건 실측):
- 기각 `0x4m4/hexstrike-ai` ★11,278 — 공격 보안 도구 MCP 150+; 오너 워크플로 외.
- 기각 `modelcontextprotocol/inspector` ★10,723 — MCP 서버 시각 테스터. 현재 MCP 작성 수요 없음(fastmcp 기각과 동일 사유). MCP 제작 시 재검토.
- 기각 `InternLM/MindSearch` ★6,916 — 멀티에이전트 웹검색 엔진; wiki Research의 Deep Research Skill·WebSearch 에이전트로 충분.
- 기각 `SimoneAvogadro/android-reverse-engineering-skill` — Android 역공학; 수요 없음.
- 기각 `trailofbits/skills` ★6,786 — 보안 리서치/감사 스킬 묶음. skill-audit와 짝이 될 여지 있어 **재검토 후보**(오늘은 1회 1개선).
- 기각 `chuspeeism/dashi-ppt-skill` — 슬라이드 생성; guizang-ppt-skill 기각과 동일.
- 기각 `jacob-bd/gemini-notebook-mcp-cli` — Gemini Notebook 전용.
- 기각 `zarazhangrui/codebase-to-course` — 코드베이스→강의 HTML; 수요 없음.
- 기각 `internet-court/internet-court-skill` — 에이전트 커머스/결제 신뢰 계층; 워크플로 외.
- 기각 `xingkongliang/skills-manager` ★3,934 — 50+ 툴 간 스킬 동기화 데스크톱 앱; 오너는 Claude Code 단일 하네스 + find-skills 보유로 실익 낮음.

- [x] G1: 스킬 파일이 존재하고 frontmatter(name/description)가 유효
  CHECK: test -f ~/.claude/skills/skill-audit/SKILL.md && head -4 ~/.claude/skills/skill-audit/SKILL.md
  EXPECT: name: skill-audit · description 1줄
  EVIDENCE: ~/.claude/skills/skill-audit/{SKILL.md,run-audit.ps1,run-audit.sh} 생성 · head -4 = frontmatter name: skill-audit + description 1줄 (실측 00:36) · 세션 스킬 목록에 skill-audit 자동 등재 확인

- [x] G2: 래퍼 스크립트로 실제 스킬 디렉토리 1개를 스캔해 정상 종료(정적 분석, 키 불필요)
  CHECK: pwsh ~/.claude/skills/skill-audit/run-audit.ps1 ~/.claude/skills/docs-search
  EXPECT: exit 0 또는 finding 보고 exit(비정상 crash 아님), 출력에 결과 요약 포함
  EVIDENCE: uv tool install --python 3.12 skillspector==2.9.6 성공(태그 v2.9.6 실재). 1차 exit=2 "Refusing to resolve a junctioned input" — ~/.claude/skills의 docs-search·find-skills가 Windows junction(→.codex/.agents)이라 도구가 거부 → 래퍼에서 LinkType→Target 1단계 해석 추가 후 재실행 exit=0(정적 분석기 전부 completed, LLM 4종 disabled_by_configuration). find-skills --format json: score 7 LOW SAFE, issues 12건 전부 RP1(미고정 npx) MEDIUM, coverage 100%. bash 래퍼도 exit=0 (실측 2026-08-23 5차)

- [x] G3: seenRepos 40건·재스캔 시 후보에 seenRepos 누출 0건
  CHECK: node evolve/trends.mjs && node -e "const s=require('./evolve/state.json');const t=require('./evolve/trends-latest.json');const S=new Set(s.seenRepos);console.log('seen',s.seenRepos.length,'leak',t.candidates.filter(c=>S.has(c.fullName)).length)"
  EXPECT: seen 40 · leak 0
  EVIDENCE: TRENDS_OK scanned=84 new=42 dup_wiki=2 dup_seen=40 · seen 40 leak 0 cand 42 (실측 2026-08-23 5차)

- [x] G4: health 전 항목 통과 + 커밋에 다른 세션 WIP 미포함
  CHECK: node evolve/health.mjs; git show --stat HEAD
  EXPECT: ALL_HEALTHY · 커밋 파일 = evolve/state.json·trends-latest.json·health-latest.json·gates/daily-2026-08-23.md
  EVIDENCE: 9종 ok, ALL_HEALTHY · http://127.0.0.1:7766/ → 200 (실측 5차) · 커밋 8adc87c show --stat = 정확히 그 4파일 (sync-claude-os.mjs·projects-data.js·logs/ 제외, 실측)

---

# Gates: daily 2026-08-23 (6차 실행) — arscontexta 지식화(c) + 트렌드 후보 검토 10건

맥락: openFindings 없음·health 9/9 ok(실측, 2026-08-22T15:41Z 스냅샷). trends-latest 후보 42건(scanned 84, dup wiki 2·seen 40) 중 상위 10건 검토. 오너의 ~/.wiki LLM Wiki·wiki-librarian 운영과 직결되는 `agenticnotetaking/arscontexta`(★3,480, 대화→개인 지식 시스템 생성 Claude Code 플러그인)를 오늘의 1개선으로 **지식화(c)** 채택. wiki grep 중복 0건 실측.
동시 작업 주의: dashboard.html(cosPanelRefresh KB 실패 표시)·projects-data.js·logs/·shot-*.png는 다른 세션 WIP → 선택 커밋.

검토 결과(채택 1 / 기각 9):
- **채택(c) `agenticnotetaking/arscontexta` ★3,480** — 대화로 사용자 사고방식을 추출해 개인 markdown 지식 시스템을 생성. LLM Wiki·wiki-librarian과 같은 문제 영역 → wiki entity로 ingest, 가져갈 아이디어 기록.
- 기각 `glitternetwork/pinme` ★3,735 — 프론트엔드 1커맨드 배포(IPFS 계열). 오너 배포 경로(Apps in Toss/console)와 무관.
- 기각 `elementalsouls/Claude-BugHunter` ★3,731 — 외부 레드팀/버그헌팅 스킬 번들. hexstrike-ai 기각과 동일 사유(공격 보안 워크플로 외).
- 기각 `isjiamu/gzh-design-skill` ★3,255 — Markdown→위챗 공중호 HTML. 수요 없음.
- 기각 `taishi-i/awesome-ChatGPT-repositories` ★3,209 — ChatGPT/OpenAI 중심 큐레이션 목록. 타 프로바이더·카탈로그성.
- 기각 `SciSharp/BotSharp` ★3,098 — .NET 멀티에이전트 프레임워크. 스택 불일치.
- 기각 `Agentchengfeng/chengfeng-videocut-skills` ★2,914 — 영상 편집 에이전트. 수요 없음.
- 기각 `dominikmartn/nothing-design-skill` ★2,741 — Nothing 디자인 언어 UI 스킬. toss-tds-design·frontend-design 보유.
- 기각 `tradermonty/claude-trading-skills` ★2,695 — 주식 트레이딩 분석 스킬. 워크플로 외.
- 기각 `jeremylongshore/claude-code-plugins-plus-skills` ★2,660 — 3,069 스킬 마켓플레이스+ccpi CLI. find-skills 보유·skills-manager 기각과 동일 사유(카탈로그 bloat).

- [x] G1: wiki entity 파일 생성·index 등재·중복 0
  CHECK: test -f ~/.wiki/entities/arscontexta.md && grep -c arscontexta ~/.wiki/index.md && ls ~/.wiki/entities | grep -ci arscontexta
  EXPECT: 파일 존재 · index 1건 이상 · entities 내 동명 파일 정확히 1개
  EVIDENCE: ~/.wiki/entities/arscontexta.md 존재(frontmatter title/aliases/source/confidence 유효, wikilink 8건) · index.md 등재 1건(§Entities L201) · entities 동명 파일 1개 · index-thin Entities 64→65·총 ~255 갱신 (실측 6차)

- [x] G2: 원장 갱신 — seenRepos 40→50, appliedImprovements에 wiki:agenticnotetaking/arscontexta, 재스캔 누출 0
  CHECK: node evolve/trends.mjs && node -e "const s=require('./evolve/state.json');const t=require('./evolve/trends-latest.json');const S=new Set(s.seenRepos);console.log('seen',s.seenRepos.length,'leak',t.candidates.filter(c=>S.has(c.fullName)).length,'applied',s.appliedImprovements.some(a=>a.id==='wiki:agenticnotetaking/arscontexta'))"
  EXPECT: seen 50 · leak 0 · applied true
  EVIDENCE: TRENDS_OK scanned=84 new=32 dup_wiki=2 dup_seen=50 · seen 50 leak 0 cand 32 applied true (실측 2026-08-23 6차) · 다음 상위: twostraws/Swift-Agent-Skills★2515, expo/skills★2444, agentuniverse-ai/agentUniverse★2334

- [x] G3: health 전 항목 통과 + http 200
  CHECK: node evolve/health.mjs
  EXPECT: ALL_HEALTHY
  EVIDENCE: 9종 ok, ALL_HEALTHY · server-dashboard/server-wiki HTTP 200 (실측 6차)

- [x] G4: 커밋에 다른 세션 WIP 미포함
  CHECK: git show --stat HEAD
  EXPECT: 커밋 파일 = evolve/state.json·trends-latest.json·health-latest.json·gates/daily-2026-08-23.md (dashboard.html·projects-data.js·logs/·shot-*.png 제외)
  EVIDENCE: 커밋 dc68e0d show --stat = 정확히 그 4파일 (dashboard.html·projects-data.js·logs/·shot-*.png 제외, 실측 6차) · push 완료
