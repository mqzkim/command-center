# Gates — 2026-09-05

오늘의 개선: zzet/gortex 도구 설치(b) — 09-04 이월 1순위. 조건: `gortex install` 자동 훅/스킬 주입 금지, 수동 MCP 등록 한정, MCP 도구 수 축소 설정, 토큰 절감 주장 smoke 실측. (지식화(c)는 09-04 완료, appliedImprovements dedup 확인 — 오늘 id는 `tool:zzet/gortex-install`로 별개.)

## G1 — 후보 검토·원장 갱신
- 조건: 오늘 trends-latest 신규 후보 상위 10~11건 검토(채택·기각 사유 기록) + 검토분 전부 `state.seenRepos` 추가 (186→197).
- 증거: ✅ 검토 11건 — 기각 10(apify-mcp-server:상용 SaaS 크레딧 의존·WebFetch 중복 / duckduckgo-mcp-server:WebSearch 내장 중복 / Gearboy:에뮬레이터 도메인 불일치 / show-me-the-money:검증 불가 수익 클레임 / claude-code-skills-zh:애그리게이터 / unifi-mcp:UniFi 미보유 / line-bot-mcp-server:LINE OA 미운영 / comfyui-mcp:ComfyUI 미사용 / claude-skill-social-post:SNS 계정 자동화 정책 리스크 / roam-code:gortex 동일 축 하위 호환[28언어·246도구], gortex entity 비교 절 채록만), 이월(c) 1(cisco-ai-defense/mcp-scanner ★1,061 — MCP 서버 보안 스캐너, skill-audit 상보 축·gortex MCP 등록으로 실익↑, 범위 확인 후 결정). node 파싱 실측 seenRepos 197 == 186+11, 중복 0.

## G2 — 설치 실증 (비침습 조건)
- 조건: (1) gortex 바이너리 설치 + `gortex --version` 실측. (2) `gortex install` 미실행 — 설치 전후 `~/.claude/settings.json` 해시 불변 + `~/.claude/skills/`에 gortex 관련 디렉토리 신규 0건. (3) MCP 수동 등록(도구 수 축소 설정 적용) 후 등록 확인. (4) smoke: command-center 인덱싱 → 심볼/그래프 조회가 실제 코드 위치를 반환.
- 증거: ✅ (1) Releases v0.63.10 `gortex_windows_amd64.zip` 직접 다운로드(scoop 미설치·install.ps1 배제), checksums.txt SHA256 일치, `C:\Users\my\.local\bin\gortex.exe` 290,829,824B(기존 PATH 등재 디렉토리, PATH 수정 0건), `gortex v0.63.10+506252c go1.26.6 windows/amd64`. (2) settings.json SHA256 사전=사후 `9EE001C1...427A` — 호출자 측 재실측 동일, skills 32개 디렉토리 불변·`*gortex*` 0건(호출자 Glob 재확인), `gortex init`도 미실행(repo 내 .mcp.json 오염 방지) — 상태 전부 `~\.gortex\` 격리. (3) per-connection 프리셋 `GORTEX_TOOLS=localization`(docs/mcp.md read-only diet set)으로 `claude mcp add --scope user gortex --env GORTEX_TOOLS=localization -- gortex.exe mcp` — JSON-RPC tools/list 실측 **14개**(전체 ~175 대비 축소, stderr `tool surface restricted (preset "localization")`), `claude mcp list` √ Connected, 수정 파일은 ~/.claude.json뿐(호출자 grep 확인). (4) track 2.56초 — files 64·nodes 1,240·edges 2,833·FTS5 427. `query symbol record`→health.mjs:24, `freshRequire`→:29 — grep 대조 라인 정확 일치, callers 조회도 top-level 호출 정확. 응답 91자/181자 vs 파일 7,432B ≈ 41~81× 축소(50× 주장 소규모 스팟체크 부합). 텔레메트리 disabled+endpoint 미설정 실측. cosign 서명 검증은 로컬 도구 부재로 생략(SHA256만).

## G3 — 파이프라인 건강
- 조건: `node evolve/health.mjs` 재실행 9/9 ok, unresolved 없음.
- 증거: ✅ 2026-09-04T22:40:12Z 재실행 — 9/9 ok·ALL_HEALTHY·exit 0(server-dashboard·server-wiki HTTP 200), health-latest.json results 전항 ok:true·unresolved []. state.json 편집 후 node require 파싱 OK(appliedImprovements 24·seenRepos 197).

## G4 — 원장·커밋
- 조건: state.json(lastRun·runs append·appliedImprovements·seenRepos) 갱신 후 커밋·푸시 성공, 커밋 sha 기록.
- 증거: ⏳
