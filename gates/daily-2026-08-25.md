# Gates: daily 2026-08-25 — build-projects.mjs 크래시 수리(앱인토스 카테고리 vs toss=null 불일치) · projects-data.js 신선화 복구

맥락: openFindings 없음·health 9/9 ok(실측 스냅샷 2026-08-24T22:30Z)이지만, `logs/refresh.log`(06:03)와 `evolve/logs/2026-08-25.log`(07:30) 모두에서 `node build-projects.mjs` exit=1 —
`TypeError: Cannot read properties of null (reading 'blockersOpen')` at build-projects.mjs:181. `projects-data.js`는 2026-08-23 03:03Z 생성본에 정지(2일 stale).
원인(실측): `C:\Users\my\workspace\app-in-toss`(모노레포 루트)는 `.git` 있음·`.launch/launch-status.json` 없음·`granite.config.ts` 없음 → 로컬 항목 `toss=null`·`isToss=false`인데,
`projects-raw.json`의 gh 메타가 `category="apps-in-toss"`·`isAppsInToss=true`라서 `category`만 apps-in-toss로 분류됨 → §6 요약 `toss.filter(p => p.toss.blockersOpen)`에서 null 역참조.
(overrides 메모 "루트 launch-status는 악어밥 낚시왕 import 잔재"가 정리되면서 잠복 버그가 드러난 것으로 추정.) health C4는 파일 파싱만 검사해 stale을 잡지 못함 — 이건 §6 자기수정 소재로 내일 이월(오늘은 기능 수리만).
플레이북 §2 정신(수리 우선)에 따라 오늘은 트렌드 채택 없음, seenRepos 불변.
동시 작업 주의: galaxy-data.js·projects-raw.json·logs/refresh.log는 run-daily 파이프라인 산출물(안전망 커밋이 수습) → 본 실행은 선택 커밋.

- [x] G1: build-projects.mjs 재실행 exit 0, app-in-toss 항목이 `toss.stage === "unverified"` 객체를 가짐(카테고리·toss 불일치 0건)
  CHECK: node build-projects.mjs && node -e "const P=require('./projects-data.js');const bad=P.items.filter(p=>p.category==='apps-in-toss'&&!p.toss);const a=P.items.find(p=>p.name==='app-in-toss');console.log('mismatch',bad.length,'app-in-toss.toss.stage',a&&a.toss&&a.toss.stage,'summary',JSON.stringify(P.summary))"
  EXPECT: exit 0 · mismatch 0 · app-in-toss.toss.stage unverified
  EVIDENCE: `node --check` EXIT=0 · `node build-projects.mjs` → "dirs scanned 81 | items 95 | visible 71 | toss 20 (launch-status 17)" EXIT=0 · mismatch 0 · app-in-toss.toss.stage unverified, note "launch-status.json 없음 — 단계는 미확정…" · summary tossWithBlockers 14 · byStage {mvp_prd 4, scaffold 2, registration_draft 4, sandbox_qa 5, released 2, unverified 3} (실측 13차, agent)
- [x] G2: projects-data.js 신선화 — generatedAt이 오늘(2026-08-25 KST; 스탬프는 UTC) 날짜, items ≥ 직전 스냅샷 수준, dashboard.html 인라인 JS + data.js 로드 정상
  CHECK: head -2 projects-data.js; node -e "const P=require('./projects-data.js');console.log(P.generatedAt,P.items.length,P.summary.toss)"
  EXPECT: 생성 2026-08-25 KST(UTC 08-24 22:xxZ 이후) · items > 0 · toss ≥ 12
  EVIDENCE: 헤더 "생성: 2026-08-24 22:34Z"(= 08-25 07:34 KST, 직전 08-23 03:03Z에서 갱신) · items 95 · toss 20 · dashboard 인라인 JS vm.Script ok, data.js keys 10 (실측 13차, agent)
- [x] G3: health 전 항목 통과 + http 200 + 수정 파일 문법
  CHECK: node --check build-projects.mjs && node evolve/health.mjs
  EXPECT: node --check exit 0 · ALL_HEALTHY · server-dashboard/server-wiki HTTP 200
  EVIDENCE: node --check EXIT=0 · health 9종 ok, ALL_HEALTHY, EXIT=0 · curl /dashboard.html 200, /wiki/index.md 200 (실측 13차, agent) · wiki ingest 디스크 실측: self-evolving-daily-loop.md:52 신규 절, layer-separated-monitoring.md:52 역링크, log.md append (grep 2026-08-25)
- [x] G4: 커밋에 파이프라인 산출물·타 세션 WIP 미포함, 원장 갱신
  CHECK: git show --stat HEAD; node -e "const s=require('./evolve/state.json');console.log(s.runs.at(-1).date,s.appliedImprovements.at(-1).id,s.consecutiveFailures,s.seenRepos.length)"
  EXPECT: 커밋 파일 = build-projects.mjs·projects-data.js·evolve/state.json·gates/daily-2026-08-25.md (galaxy-data.js·projects-raw.json·logs/ 제외) · runs 마지막 2026-08-25 · appliedImprovements 마지막 fix:build-projects-toss-null · failures 0 · seenRepos 102(불변)
  EVIDENCE: 1차 커밋 0770f7d — git show --stat 파일 4개: build-projects.mjs·projects-data.js·evolve/state.json·gates/daily-2026-08-25.md (파이프라인 산출물 제외) · 원장 runs 마지막 2026-08-25 / appliedImprovements 마지막 fix:build-projects-toss-null / consecutiveFailures 0 / seenRepos 102 · ledger sha 후속 커밋 별도 (실측 13차, agent)

수정 내용:
- `build-projects.mjs` §5 항목 조립: `isToss`에 gh 메타(`gh.isAppsInToss` / `gh.category === "apps-in-toss"`)를 포함 → 로컬 clone에 launch-status·granite 설정이 없어도 `unverifiedToss(...)`로 "미확정" 객체를 만든다(gh-only 경로와 동일 의미론). 추정 금지 원칙 유지 — note에 "launch-status.json 없음"을 그대로 남김.
- §6 요약: `p.toss?.blockersOpen`·`p.toss?.source`·`p.toss?.stage` 옵셔널 체이닝으로 방어 (다른 경로에서 불일치가 재발해도 빌드가 죽지 않고 대시보드가 갱신됨).

다음 후보 1줄: §6 자기수정 — health C4에 신선도 게이트 추가(`projects-data.js generatedAt` < `projects-raw.json generatedAt`이면 FAIL→재빌드, 실패 시 openFindings escalation; refresh.log exit≠0도 검출). 트렌드 다음 상위: Prat011/awesome-llm-skills★1,679 · Shpigford/chops★1,556 · disler/claude-code-hooks-multi-agent-observability★1,522.
