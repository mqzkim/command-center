# Gates — 2026-09-15

오늘의 개선(예정): yuting0624/antigravity-for-claude-code 지식화(c) — 09-12부터 이월된 1순위. Gemini CLI를 Claude Code의 협업 서브에이전트로 쓰는 크로스모델 위임 축(claudex-loop 인접). 신규 후보 9건은 이월 1건(snyk/agent-scan) 외 전건 기각 예정(아래 §검토 기록).

## G1 — 실측 조사 (라이선스·활동·구조)

조건: gh CLI 실측으로 star·license **본문**(NC 조항 유무)·pushed_at·릴리스·contributors(단독 저자 여부)·파일 트리(실행 표면) 확인. 추정 수치 0건.

증거: **통과** (2026-09-15 gh CLI 실측, background agent) — ★338(09-12 스캔 329에서 증가)·forks 30·open_issues 6·created 2026-06-19·pushed 2026-09-14T13:50Z(활발)·릴리스 v0.25.0~v0.25.2·contributors yuting0624 77/85≈90.6% 실질 단독. **LICENSE 본문 = 표준 MIT 전문 (c) 2026 linyuting, NC 조항 0, API spdx MIT와 일치**. 구조: Claude Code 플러그인(MCP 서버 없음, CLI 래퍼) — commands 10종·antigravity-delegate 서브에이전트·SessionStart 훅(agy 헬스체크+비용 라우팅 정책 주입)·SKILL.md 2개·scripts .sh 7+.py 2·bin shim 9·CI 8 워크플로(shellcheck·zizmor·quorum-review). "Claude=지휘자/Gemini=실행자" 비용 라우팅, A/B 실측 문화(−27%/−64% "estimates" 자기 명시), 자동 전면 라우팅은 실측상 손해라 배제. **설치(b) 기각 확정**: 전제조건 `agy` CLI+Google/Vertex 인증, 네이티브 Windows(Git Bash/MSYS) 비권장 자기 선언(ConPTY 없이 `agy -p` hang, issue #6), bash 하드코딩 훅. 지식화(c)만 채택.

## G2 — dedup 게이트

조건: `~/.wiki/`에 antigravity-for-claude-code 기존 페이지 없음 + `state.appliedImprovements`에 해당 id 없음 확인.

증거: **통과** — Grep `antigravity-for-claude-code|yuting0624`(case-insensitive) `~/.wiki/` 전체 0건, Glob `**/*antigravity*` 0건 (2026-09-15 본 세션 실측). state.json appliedImprovements 전체 Read — 해당 id 없음(09-12/09-13/09-14 이월 note에만 후보로 언급).

## G3 — wiki ingest 디스크 실측

조건: wiki-librarian agent 완료 후 entity 파일이 디스크에 실존(Read/Grep으로 호출자 재실측), index.md/index-thin.md 카운트 갱신, 역링크 ≥2곳(claudex-loop·self-evolving-daily-loop 후보).

증거: **통과** — wiki-librarian 완료 후 호출자 Grep/ls 재실측(2026-09-15): ① `entities/antigravity-for-claude-code.md` 디스크 실존(5,466B, claudex-loop 비교 표 포함) ② 역링크 3곳 — `entities/claudex-loop.md:94`(작성자≠심사자 vs 지휘자≠실행자)·`concepts/self-evolving-daily-loop.md:145` evolve 33차 절·`concepts/evidence-truth-framework.md:80`(A/B "estimates" 자기 한정 문화) ③ `index.md:250` 등재 + Entities 83→84, `index-thin.md:11` 핵심 목록 포함 + 총 ~295, `:55` 변경 이력 ④ `log.md:3` ingest 항목.

## G4 — 검증·원장·커밋

조건: `node evolve/health.mjs` 전항 ok, state.json(lastRun·runs·appliedImprovements·seenRepos+9) 갱신 + node 파싱 검증, 커밋 sha 확보·push.

증거: **통과** — `node evolve/health.mjs` 9/9 ok·healed 0·ALL_HEALTHY (커밋 직전 실행 실측). state.json node 파싱 검증 ok(seenRepos 294=285+9·runs 30(31→32에서 최고령 2건 트림, 플레이북 '최근 30개 유지' 준수)·appliedImprovements 34, 말단 id `wiki:yuting0624/antigravity-for-claude-code`). 1차 커밋 **2fa8a18** push 성공(`00cf970..2fa8a18 master`). sha 원장 기입은 본 ledger 커밋.

## 검토 기록 — 2026-09-15 신규 후보 9건

1. **kucherenko/jscpd** (★6,209, mcp) — 기각. copy/paste 중복 코드 검출 CLI — 오너 워크플로에 중복 검출 반복 수요 없음 + code-intel 도구 축은 설치 완료된 gortex로 충족(roam-code/tokensave/ripwire/sigmap 처리 전례).
2. **snyk/agent-scan** (★3,043, mcp) — **이월(c)**. AI 에이전트·MCP 서버·agent skills 보안 스캐너 = 오너의 skill-audit(SkillSpector, 설치 전 정적)·mcp-scanner(Cisco, 등록 MCP 표면) 축과 직접 겹치는 제3의 대형 벤더 구현 — 3자 비교 지식화 가치. 차기 후보로 이월.
3. **0xMassi/webclaw** (★2,343, mcp) — 기각. 웹 콘텐츠 추출/크롤 = 내장 WebFetch/WebSearch 중복 축(duckduckgo/tavily/apify/us-crw 기각 전례).
4. **plugin87/ux-ui-agent-skills** (★1,304, skills) — 기각. 디자인 시스템·컴포넌트·WCAG 게이트 스킬 = 기존 toss-tds-design·refactoring-ui·frontend-design과 중복(tastemaker/qiaomu-design 기각 전례).
5. **erikdarlingdata/PerformanceMonitor** (★497, mcp) — 기각. SQL Server/Postgres 성능 모니터링 — 해당 DB 미사용(pg-aiguide/neon 기각 전례).
6. **fabio-rovai/open-ontologies** (★497, mcp) — 기각. RDF/OWL 온톨로지 추론 엔진 — 온톨로지 워크플로 부재, 도메인 불일치.
7. **Brhiza/mingyu** (★413, mcp) — 기각. 사주·점술 도구 — 도메인 불일치.
8. **Extelligence-ai/bagel** (★396, mcp) — 기각. 로보틱스·드론·IoT 데이터 조회 — 도메인 불일치.
9. **natsukium/mcp-servers-nix** (★300, mcp) — 기각. Nix 기반 MCP 설정 프레임워크 — 오너 네이티브 Windows 환경과 불일치(WSL 전용 축 기각 전례).
