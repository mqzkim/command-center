# Gates: daily 2026-08-26 — §6 자기수정: health C4 신선도 게이트(valid≠fresh) + refresh.log 빌드 실패 검출

맥락: openFindings 없음·health 9/9 ok(실측 스냅샷 2026-08-25T22:30Z). 어제(08-25) 수리 과정에서 드러난 감시 공백을 이월 처리: health C4는 `projects-data.js`가 **파싱되기만 하면** ok로 판정해, 08-23~08-25 2일간 stale인 상태를 9/9 ok로 놓쳤다(`build-projects.mjs` exit=1이 `logs/refresh.log`·`evolve/logs/*.log`에만 남고 self-heal이 보지 않았음). 오늘은 §6 자기수정만 수행 — 트렌드 채택 없음(상위 10건 검토·seenRepos 원장 기록만, 채택은 내일로 이월).
동시 작업 주의: galaxy-data.js·projects-data.js·projects-raw.json·logs/refresh.log는 run-daily/refresh 파이프라인 산출물(안전망 커밋이 수습) → 본 실행은 선택 커밋.

- [ ] G1: 수정 스크립트 문법 + 정상 상태에서 health 전 항목 통과(신규 게이트가 현재 신선한 데이터를 stale로 오판하지 않음)
  CHECK: node --check evolve/health.mjs && node evolve/health.mjs
  EXPECT: node --check exit 0 · projects-data ok(healed=false, note "") · ALL_HEALTHY · server-dashboard/server-wiki HTTP 200
  EVIDENCE: node --check exit 0 · health exit 0: `ok projects-data`(note "") · server-dashboard/server-wiki HTTP 200 · ALL_HEALTHY · health-latest.json projects-data {ok:true,healed:false,note:""}, unresolved [] (실측 14차, agent, UTC 2026-08-25T22:36Z)
- [x] G2: 유도 stale 시나리오에서 자동 복구 — generatedAt을 3일 전으로 바꾼 projects-data.js에 대해 C4가 FAIL→재빌드→HEALED로 응답하고 결과 파일이 오늘 스탬프로 갱신됨
  CHECK: cp projects-data.js /tmp/pd.bak; node -e "const f=require('fs');let s=f.readFileSync('projects-data.js','utf8');s=s.replace(/\"generatedAt\": \"[^\"]+\"/,'\"generatedAt\": \"2026-08-23T00:00:00Z\"');f.writeFileSync('projects-data.js',s)"; node evolve/health.mjs; node -e "console.log(require('./projects-data.js').generatedAt)"
  EXPECT: 출력에 `HEALED projects-data — 재빌드(stale ...h)` · 종료 "HEALED — 전 항목 정상" · generatedAt이 실행 시각(UTC) · health-latest.json projects-data.healed=true
  EVIDENCE: 변조 generatedAt 2026-08-23T00:00:00Z → health exit 0: `HEALED projects-data — 재빌드(stale 71h)` · `HEALED — 전 항목 정상 (자동 복구 수행됨)` · 재빌드 후 generatedAt 2026-08-25 22:35Z(실행 시각 22:36Z) · health-latest projects-data {ok:true,healed:true,note:"재빌드(stale 71h)"} · .g2bak 삭제 (실측 14차, agent)
- [x] G3: 유도 빌드 실패 시나리오에서 escalation — refresh.log 마지막 `build-projects exit=1` 라인만으로는 FAIL 판정하지 않고 재빌드가 성공하면 HEALED(로그는 과거 증거·현재 상태는 재빌드로 검증), 재빌드 자체가 실패하면 FAIL + state.openFindings에 남음
  CHECK: (a) refresh.log에 `build-projects exit=1` 임시 append 후 node evolve/health.mjs → 원복; (b) build-projects.mjs를 일시적으로 exit 1로 바꾸고(예: 첫 줄에 `process.exit(1)`) 동일 실행 → 원복 + 원복 후 health 재실행으로 openFindings 비움
  EXPECT: (a) `HEALED projects-data — 재빌드(refresh.log build-projects exit=1)` (b) `FAIL projects-data — ... 재빌드 실패` + UNRESOLVED 1 + state.openFindings[0].check==="projects-data" · 원복 후 ALL_HEALTHY·openFindings []
  EVIDENCE: (a) refresh.log append 후 health exit 0: `HEALED projects-data — 재빌드(refresh.log build-projects exit=1)` → 로그 원복, `git diff logs/refresh.log | grep '^+.*exit=1'` 없음 (b) build-projects.mjs 첫 줄 `process.exit(1)` + stale 변조 → health exit 1: `FAIL projects-data — stale 71h 재빌드 실패 Command failed: node build-projects.mjs` · `UNRESOLVED 1: projects-data` · state.openFindings [{check:"projects-data",…,at:2026-08-25T22:36:43Z}] → 원복(`git diff build-projects.mjs` 비어 있음) 후 재실행 ALL_HEALTHY·openFindings [] (실측 14차, agent). 후속 보강: 재빌드 실패 note에 stderr 마지막 줄 포함(e.stderr||e.message) — 커밋 agent가 node --check·health 재실측.
- [x] G4: 커밋에 파이프라인 산출물·타 세션 WIP 미포함, 원장 갱신, 트렌드 검토 원장
  CHECK: git show --stat HEAD; node -e "const s=require('./evolve/state.json');console.log(s.runs.at(-1).date,s.appliedImprovements.at(-1).id,s.consecutiveFailures,s.seenRepos.length)"
  EXPECT: 커밋 파일 = evolve/health.mjs·evolve/evolve.md(있다면)·evolve/state.json·gates/daily-2026-08-26.md (galaxy-data.js·projects-data.js·projects-raw.json·logs/ 제외) · runs 마지막 2026-08-26 · appliedImprovements 마지막 self:health-freshness-gate · failures 0 · seenRepos 102→112
  EVIDENCE: 커밋 전 재검증 node --check exit 0 · health 9/9 ALL_HEALTHY exit 0 · 본 커밋 ae134cb — git show --stat 파일 4개: evolve/evolve.md(+1)·evolve/health.mjs(+36)·evolve/state.json·gates/daily-2026-08-26.md (파이프라인 산출물 6종 스테이징 제외) · 원장 sha 커밋 d9e6640 · push bdc7f02..d9e6640 master exit 0 · 원장 실측 "2026-08-26 ae134cb self:health-freshness-gate 0 112" (실측 14차, agent). 본 EVIDENCE 줄 자체는 커밋 이후 기록 → run-daily 안전망 커밋이 수습.

수정 내용(예정):
- `evolve/health.mjs` C4: 파싱 검사에 더해 (1) `generatedAt` 부재/36h 초과 → stale, (2) `projects-raw.json.generatedAt`(gh 스냅샷)이 더 최신 → stale, (3) `logs/refresh.log` 마지막 `build-projects exit=N`이 0이 아니면 → stale 로 판정하고 `node build-projects.mjs` 재빌드 후 (1)(2)만 재검사(로그는 과거 기록이라 재빌드 성공 시 무시). 재빌드도 실패하면 FAIL → `state.openFindings` escalation(기존 경로).
- 헤더 주석: "체크 9종" 유지(항목 수 불변, C4 강화).

검토 결과(오늘 채택 0 / 채택 후보 3 · 기각 7) — `gh api` 실측(agent, 2026-08-26). 10건 모두 ~/.wiki 미등재·seenRepos 미기재 확인:
- **채택 후보(b) 내일 1순위** `danyuchn/asd-ste100-skill` ★1,433 (push 08-13, MIT) — ASD-STE100 통제언어 규칙을 에이전트용 영문(툴 설명·에러 메시지·서브에이전트 지시문) 재작성에 전용한 단일 스킬, 의존성 0. 오너 자산과 비중복, skill-audit(SkillSpector)와 보완. 추정: 한국어 주 워크플로라 적용 빈도 중간.
- 채택 후보(c) `NeoLabHQ/context-engineering-kit` ★1,387 (push 08-25, GPL-3.0) — reflexion·SADD(judge 서브에이전트)·SDD·DDD 플러그인 마켓. superpowers와 기능 중복 커서 설치는 비추천, "신뢰도 vs 토큰 오버헤드" 계층표는 5-Layer Validation concept 비교 채록 가치. GPL 재배포 주의.
- 채택 후보(c, 약함) `daymade/claude-code-skills` ★1,350 (push 08-25, MIT) — skill-creator 강화 포크 번들. 스킬 충돌 감지→SessionStart 라우팅 훅·동시 세션 git-ref 베이스라인 패턴만 Hook System concept 1단락감. 번들 강제 설치라 스킬 채택 부적합.
- 채택 후보(b, 후순위) `skills-directory/skill-codex` ★1,414 (push 07-19, MIT) — `codex exec` 위임 플러그인. codex CLI 설치 확인됐으나 orca-cli 스킬이 "spawn codex in worktree" 커버로 부분 중복, 모델명 목록 stale 구조.
- 기각 `Prat011/awesome-llm-skills` ★1,686 (push 07-14, README Apache-2.0) — awesome-list 카탈로그. find-skills와 중복, 6주 무갱신.
- 기각 `Shpigford/chops` ★1,558 (push 08-23, FSL-1.1-MIT) — macOS 15+ SwiftUI 앱. Windows 11 실행 불가.
- 기각 `disler/claude-code-hooks-multi-agent-observability` ★1,526 (push 02-08, 라이선스 없음) — Hook→Bun→SQLite→Vue 대시보드. FailproofAI entity·Layer-Separated Monitoring이 상위 호환, 6.5개월 정지, 08-24 기각 hooks-mastery 동일 저자 계열.
- 기각 `Pluviobyte/rnskill` ★1,381 (CC-BY-NC-4.0) — 중국어 콘텐츠 제작 스킬 56종. 무관·NC 라이선스.
- 기각 `MaJerle/c-code-style` ★1,339 (MIT) — C 코딩 규칙. 오너 스택에 C 없음.
- 기각(참고) `truongduy2611/app-store-preflight-skills` ★1,326 (push 05-29, MIT) — iOS App Store 리젝 사전 점검. 출시 채널은 Apps in Toss 콘솔, apps-in-toss-qa가 동역할. rules/*.md 분리 구조만 참고.

다음 후보 1줄: (b) `danyuchn/asd-ste100-skill` 스킬 채택 — 의존성 0·MIT·비중복·skill-audit 시너지. 차순위 (c) context-engineering-kit 지식화. 자기수정 차기: galaxy-data.js에도 동일 신선도 게이트(헤더 스탬프 파싱, C3).
