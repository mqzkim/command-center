# Gates — 2026-09-03

오늘의 개선(예정): Agents365-ai/drawio-skill 스킬 채택(b) — 09-02 이월 차기 1순위.
채택 불가 판정 시 폴백: (c) 지식화 또는 차순위 후보.

## G1 — 실측 조사

- [x] repo 메타 실측: star ≥ 500, LICENSE 실체(파일 본문 기준, API 표기 아님), pushed 최근성, 의존성(오프라인/Python 여부), Windows 호환성 판단 근거 기록
- 증거: ★8,975 · LICENSE 본문 표준 MIT 전문(NC 조항 없음, API 표기와 일치) · pushed 2026-09-01(v3.2.0, open issue 0) · requirements.txt/pyproject.toml 부재 — 핵심 5스크립트 import 전수 확인 결과 100% stdlib · Windows: `/tmp`·`shell=True`·`python3` 하드코딩 없음, `sys.executable`/`shutil.which`/`pathlib` 기반, `metadata.platforms`에 windows 명시, troubleshooting.md에 native Windows 절 실재, Windows 관련 이슈 3건 전부 closed. 주의점: manual clone 시 SKILL.md 2단 중첩 → `skills/drawio-skill/` 서브폴더만 복사로 회피.

## G2 — 프론트매터 안전성 (SKILL.md 콜론 함정)

- [x] 설치되는 모든 SKILL.md frontmatter가 yaml.safe_load 통과 (description에 ": " 없음 확인, memory: skill-frontmatter-yaml-colon-trap)
- 증거: `yaml.safe_load` OK — keys `[name, description, license, allowed-tools, metadata]`, `COLON-TRAP: False` (description 532자 plain scalar에 ": " 미포함).

## G3 — skill-audit

- [x] skill-audit 스캔 exit 0 / SAFE (오탐은 사유 명시 baseline 억제 후 재스캔 확인)
- 증거: SkillSpector v2.9.6 --no-llm 1차 63/100 HIGH(32건) → repo 동봉 `.skillspector-baseline.json`은 v1 포맷이라 exit 2 비호환 → 32건 전수 육안 검토(AST4/OH1: `subprocess.run([sys.executable, ...])` shell 미사용 sibling 호출 = 핵심 설계 · P2: XML 예제 주석 · E1: `_ALLOWED_HOSTS` GitHub 3호스트 제한 manifest 갱신) 전부 오탐 판정, 사유 명시 v2 baseline `.skillspector-baseline.local.json` 재생성 → 재스캔 **exit 0, 0/100 LOW, SAFE**, coverage 98.7%.

## G4 — 동작 실측 + health

- [x] claude -p 실측 1회로 스킬 트리거·출력 확인 (또는 구조 검증 근거)
- [x] `node evolve/health.mjs` 9/9 통과, 실패 상태 커밋 없음
- 증거: `diagramctl.py --help` exit 0 · 노드 3/엣지 2 graph JSON → `build` exit 0, `out.drawio` 2,536B 유효 `<mxfile>` XML 생성(stdlib-only 확인) · `claude -p` exit 0, 스킬 레지스트리 등재·description 일치 요약 반환. health 재실행 증거는 커밋 전 기록.
