# Gates: L1 — 대시보드 ⇄ Claude OS 라이브 연결

Scope: dashboard.html·data.js·claude-os .env(CORS)만 수정. PLAN.md 허브 계약 준수.

- [x] L1-1: CORS — claude-os `.env` ALLOWED_ORIGINS에 7766 추가 + 재기동 후, Origin 7766 프리플라이트/요청에 access-control-allow-origin 헤더가 온다
  CHECK: curl -s -D - -o NUL -H "Origin: http://localhost:7766" http://localhost:8051/health
  EXPECT: /access-control-allow-origin: http:\/\/localhost:7766/
  EVIDENCE: access-control-allow-origin: http://localhost:7766 | vary: Origin
  NOTE(실측 2026-08-23 00:18 KST): `.env` 16행 ALLOWED_ORIGINS 끝에 `,http://localhost:7766,http://127.0.0.1:7766` 추가 → stop.ps1/start.ps1 재기동(server/worker/frontend 재생성, "Health: healthy") → `curl -D - -H "Origin: http://localhost:7766" /health` 응답에 `access-control-allow-origin: http://localhost:7766` + `access-control-allow-credentials: true`. OPTIONS 프리플라이트(POST, content-type)도 200 + allow-origin 7766 + `access-control-allow-headers: content-type`. 5173=200, 8051 /health healthy 확인. 이후 다른 세션이 claude-os를 여러 차례 재기동했으나(00:49·00:56 pids 변경) .env 기반이라 CORS 헤더 유지 확인(00:53 재검).

- [x] L1-2: 갤럭시 APPLICATIONS 링에 "Claude OS" 헥사곤 — data.js apps 항목 추가, 클릭 상세에 라이브 health(status·sqlite tables·ollama models)·KB 수·프로젝트 수, "5173 열기"·"8051 health" 버튼. claude-os 꺼져 있으면 "오프라인" 상태를 명시 렌더(에러 0)
  CHECK: node -e "const s=require('fs').readFileSync('C:/workspace/command-center/dashboard.html','utf8');const d=require('C:/workspace/command-center/data.js');console.log(/8051/.test(s)&&d.galaxy.orbits[2].apps.some(a=>/Claude OS/i.test(a.n))?'APP_OK':'BAD')"
  EXPECT: APP_OK
  EVIDENCE: APP_OK
  NOTE(실측): data.js `galaxy.orbits[2].apps[0]` = `{ n: "Claude OS", d: "...", live: "claude-os" }` → 노드 kind APP·hexa, path "http://localhost:8051 · http://localhost:5173". `openDetail` 끝에 `if (n.live === "claude-os") cosAppDetail(n, .extra)` 분기. 헤드리스 CDP(`gates/l1-shot.mjs ?open=Claude%20OS`) DETAIL_EXTRA 실측: "healthy · sqlite healthy · tables 8 · ollama healthy · models 7 · KB 6 · 프로젝트 1 / PROJECTS command-center C:/workspace/command-center / KNOWLEDGE BASES · 6 (command-center-project_memories·project_index·project_profile·knowledge_docs·launch-deck-project_memories·demo-kb) / 실측 2026-08-22T15:18:57 · GET /health, /api/kb, /api/projects / [5173 열기][8051 health]". 스크린샷 `shot-cos-app.png` Read 판독: 우측 상세 패널에 APP · Claude OS, 칩 healthy/sqlite/ollama/KB 6/프로젝트 1, 하단 버튼 2개, 하단 갤럭시에 "Claude OS" 헥사곤 라벨 표시. 오프라인(`?open=Claude%20OS&cosPort=8059` 죽은 포트): DETAIL_EXTRA "오프라인 — claude-os(8051) 응답 없음 — C:\workspace\claude-os\start.ps1 로 기동 [5173 열기][8051 health]", data-errors 0. 8051 실제 다운 시점(00:53, 타 세션 재기동 중)에도 동일 "오프라인" 렌더 + errors 0 실측.

- [x] L1-3: 드로어 "CLAUDE OS" 패널 — health 상태, KB 4개(command-center-*)별 문서 수(/api/kb/{kb}/stats 또는 documents), project_memories 최근 5건 제목, 검색 입력창(Enter → POST /api/kb/search-all top_k 8) 결과 리스트(kb명·score·본문 120자). 전부 런타임 fetch, 하드코딩 0
  EVIDENCE: drawerBody 템플릿에 `<h2 class="cos-h">CLAUDE OS <span id="cosState">` + `#cosPanel`(#cosKb · 기억 최근 5 #cosMem · `#cosQ` search input · #cosRes · 5173/8051 버튼). `cosPanelRefresh()`: GET /health → /api/kb(command-center-* 필터) → 각 /api/kb/{kb}/stats 병렬 → /api/kb/command-center-project_memories/documents(upload_date 정렬 top5). `#cosQ` keydown Enter → `cosSearchInto(#cosRes, q, 8)` = POST /api/kb/search-all {query, top_k:8} → li당 title(metadata.title|filename) · kb_name · score(3자리) · text 120자. 하드코딩 수치 0(grep "문서 60|문서 0 · 청크 0" = 0건). 헤드리스 CDP 실측(`node gates/l1-shot.mjs "?brief=1&q=nailmap"` 2026-08-23 00:57): COS_PANEL = "project_memories 문서 0 · 청크 0 / project_index 문서 59 · 청크 88 / project_profile 문서 0 · 청크 0 / knowledge_docs 문서 570 · 청크 5247 / 기억 0건 — remember this… / [결과] Apps in Toss Front-Loaded Launch Contract · command-center-knowledge_docs · score 0.717 · 같은 프로필을 반복하지 않고… / nail-map · command-center-project_index · 0.685 / nailmap · command-center-project_index · 0.685 / nailmap · project_index · 0.636 / Wiki Log · knowledge_docs · 0.521 …" (KB 문서 수는 L2/L3 병렬 인제스트로 실행마다 변함: project_index 59~62, knowledge_docs 63→92→120→570). 스크린샷 `shot-cos-search.png` Read 판독: 드로어 CLAUDE OS 섹션에 "healthy · sqlite healthy · ollama 2 models", KB 4행(문서·청크), 기억 섹션, 검색창 "nailmap", 결과 5건+ 카드(제목/kb·score/본문 발췌) 렌더 확인. `?q=` URL 파라미터로 자동 검색+스크롤 기능 추가(헤드리스 판독용). 타임아웃: 인제스트 중 /health 6~18s·search-all 8~10s 실측 → 3s는 허위 오프라인이라 30s로 설정, 타임아웃은 "응답 지연"·연결실패는 "오프라인"으로 구분 렌더(둘 다 에러 0).

- [x] L1-4: 프로젝트 상세 패널에 "Claude OS 검색" 버튼 — 프로젝트명으로 search-all(kb_filter 없음) 호출, 결과 5건 인라인 표시(project_index 문서가 있으면 포함됨)
  EVIDENCE: `projectHtml(p)` 끝에 `<h4>CLAUDE OS</h4><button data-cos-search="${p.name}">Claude OS 검색</button><div class="cos-inline" data-cos-for=...>` 추가, `#detail` 클릭 위임에서 `cosSearchInto(box, name, 5)`(kb_filter 없음, top_k 5) 호출. `?open=<proj>&cos=1` 파라미터로 자동 클릭. 헤드리스 CDP 실측(`?view=projects&open=nailmap&cos=1`, 00:55): DETAIL_COS_INLINE = "Apps in Toss Front-Loaded Launch Contract · command-center-knowledge_docs · score 0.717 … / nail-map · command-center-project_index · 0.685 (# nail-map keywords…) / Wiki Log · knowledge_docs · 0.521 / ClawDev Bot 스펙 · knowledge_docs · 0.501 / Multi-LLM Capability Research Substrate · knowledge_docs · 0.500" = 5건, project_index 문서 포함. 스크린샷 `shot-cos-project.png` Read 판독: nailmap 상세 패널 하단 "CLAUDE OS" 헤더 + [Claude OS 검색] 버튼 + 첫 결과 카드(Apps in Toss Front-Loaded Launch Contract · command-center-knowledge_docs · score 0.717 · 본문 발췌) 렌더 확인. 오프라인(cosPort=8059): 인라인 "오프라인 — claude-os(8051) 응답 없음", errors 0.

- [x] L1-5: 자족성·문법·에러 0 유지 + 헤드리스 스크린샷 판독(드로어 CLAUDE OS 패널, 검색 결과 렌더). claude-os 켜진 상태/끈 상태 모두 data-errors 0
  CHECK: node C:/workspace/command-center/check-dashboard.mjs
  EXPECT: /SELF_CONTAINED_SYNTAX_OK 200 200/
  EVIDENCE: SELF_CONTAINED_SYNTAX_OK 200 200 | TOSS 16 15 TOSS_OK
  NOTE(실측): 외부 리소스 0(`fetch`는 localhost:8051 런타임 호출만, 전부 `cosFetch` = try/catch + AbortController 타임아웃 + 비2xx→null). 켜진 상태: `gates/l1-shot.mjs`(CDP, Runtime.evaluate로 `body.dataset.errors`) 3개 뷰(?brief=1&q=nailmap / ?open=Claude%20OS / ?view=projects&open=nailmap&cos=1) 모두 "ERRORS 0 … ERRORS_OK". 끈 상태: 동일 3개 뷰에 `&cosPort=8059`(죽은 포트) → 모두 "오프라인" 렌더 + ERRORS 0; 추가로 타 세션 재기동으로 8051이 실제 다운된 시점(00:53, curl 000)에도 3개 뷰 ERRORS 0. 참고: 공유 claude-os를 L2/L3 인제스트 중에 직접 stop 하면 형제 리프가 깨지므로 죽은 포트 방식 + 실제 다운 시점 관측으로 대체. 스크린샷 Read 판독: shot-cos-search.png(드로어 CLAUDE OS 패널+검색 결과), shot-cos-project.png(프로젝트 상세 인라인 결과), shot-cos-app.png(헥사곤 상세), shot-cos-offline.png(드로어 "CLAUDE OS 오프라인 — claude-os(8051) 응답 없음 (start.ps1 필요)" + 검색창 아래 "오프라인" 문구). 헤드리스 주의: `--virtual-time-budget`는 펜딩 fetch 중 chrome 종료가 지연되어 `--dump-dom`은 되나 `--screenshot`은 파일 미생성 → CDP 스크립트(l1-shot.mjs)로 대체. 주의: 다른 세션이 dashboard.html을 동시 편집 중(☰ MENU·검색·위키 뷰어) — 모든 Edit는 좁은 앵커로만 적용, 편집마다 check-dashboard 재실행.
